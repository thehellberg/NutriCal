import { ScrollView } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function RecipeAdd() {
  return (
    <SafeAreaView>
      <ScrollView className="h-full"></ScrollView>
    </SafeAreaView>
  )
}
